import React, { Component } from 'react';
import { Animated, View } from 'react-native';

class Deck extends Component {
    render() {
        return (
            <View />
        );
    }
}

export default Deck;